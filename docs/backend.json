
{
  "entities": {
    "Article": {
      "title": "Article",
      "description": "学内ニュースやnote連動記事。",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "articleType": { "type": "string", "enum": ["Standard", "Note"] },
        "content": { "type": "string" },
        "noteUrl": { "type": "string", "format": "uri" },
        "categoryId": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "publishDate": { "type": "string", "format": "date" },
        "mainImageUrl": { "type": "string", "format": "uri" },
        "isPublished": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "title", "articleType", "categoryId", "publishDate", "isPublished"]
    },
    "Ad": {
      "title": "Ad",
      "description": "バナー広告データ。",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "imageUrl": { "type": "string", "format": "uri" },
        "linkUrl": { "type": "string", "format": "uri" },
        "clickCount": { "type": "number" },
        "startDate": { "type": "string" },
        "endDate": { "type": "string" }
      }
    },
    "HeroImage": {
      "title": "HeroImage",
      "description": "トップページの背景画像。",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "imageUrl": { "type": "string", "format": "uri" },
        "title": { "type": "string" },
        "order": { "type": "number" }
      }
    },
    "Inquiry": {
      "title": "Inquiry",
      "description": "お問い合わせメッセージ。",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "subject": { "type": "string" },
        "message": { "type": "string" },
        "isRead": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "MaintenanceSettings": {
      "title": "MaintenanceSettings",
      "description": "メンテナンスモード設定。",
      "type": "object",
      "properties": {
        "isMaintenanceMode": { "type": "boolean" },
        "maintenanceMessage": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    },
    "PresidentMessage": {
      "title": "PresidentMessage",
      "description": "会長挨拶設定。",
      "type": "object",
      "properties": {
        "authorName": { "type": "string" },
        "authorImageUrl": { "type": "string", "format": "uri" },
        "content": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/articles/{articleId}": {
      "schema": { "$ref": "#/entities/Article" },
      "description": "記事。公開読み取り。ログイン者は全権限。"
    },
    "/ads/{adId}": {
      "schema": { "$ref": "#/entities/Ad" },
      "description": "広告。公開読み取り。ログイン者は全権限。"
    },
    "/hero-images/{imageId}": {
      "schema": { "$ref": "#/entities/HeroImage" },
      "description": "ヒーロー画像。ログイン者は全権限。"
    },
    "/inquiries/{inquiryId}": {
      "schema": { "$ref": "#/entities/Inquiry" },
      "description": "お問い合わせ。ログイン者は全権限。"
    },
    "/settings/maintenance": {
      "schema": { "$ref": "#/entities/MaintenanceSettings" },
      "description": "メンテナンス設定。公開読み取り。ログイン者は全権限。"
    },
    "/settings/president-message": {
      "schema": { "$ref": "#/entities/PresidentMessage" },
      "description": "会長挨拶設定。公開読み取り。ログイン者は全権限。"
    }
  }
}
