rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 記事、画像、広告、設定などのコレクションは誰でも読み取り可能にする
    // 書き込み（作成・更新・削除）は認証済みユーザーのみ許可する
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
