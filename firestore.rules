rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 開発用：ログインなしで全てのコレクションを読み書き可能に設定
    // ※後ほどログインシステムを統合する際に元のルールに戻します
    match /{path=**} {
      allow read, write: if true;
    }
  }
}
