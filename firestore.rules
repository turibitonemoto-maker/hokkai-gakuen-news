
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // すべてのデータに対して、ログイン（認証）しているユーザーのみが閲覧・書き込みを許可される
    // ログインしていない一般ユーザー（読者）向けの公開は一切行わない設定
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
