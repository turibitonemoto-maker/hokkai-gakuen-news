rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 全てのドキュメントに対して、誰でも読み取り可能にする
    // 書き込み（作成・更新・削除）は認証済みユーザーのみ許可する
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}