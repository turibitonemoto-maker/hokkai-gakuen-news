
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 全てのドキュメントに対して、読み取りは一般公開、書き込みは認証済みユーザーのみに制限
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
