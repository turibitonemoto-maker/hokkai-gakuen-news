rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 全てのコレクションに対して読み取り（取得・一覧）を全公開し、
    // 書き込み（作成・更新・削除）を認証済みユーザーのみに制限します。
    match /{document=**} {
      allow read, list: if true;
      allow write: if request.auth != null;
    }
  }
}